Decode1 dec;

float sh[4][4];

for(int i; i < sh.size(); i++)
{
    for(int j; j < sh[0].size(); j++)
    {
        Math.random2f(-1.0,1.0) => sh[i][j];
        cherr <= sh[i][j] <= " ";
    }
    cherr <= IO.newline();
}

dec.placement(sh);